<%= form_for(@organizations_roster) do |f| %>
  <% if @organizations_roster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organizations_roster.errors.count, "error") %> prohibited this organizations_roster from being saved:</h2>

      <ul>
      <% @organizations_roster.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :individual_id %><br />
    <%= f.number_field :individual_id %>
  </div>
  <div class="field">
    <%= f.label :organization_id %><br />
    <%= f.number_field :organization_id %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.date_select :start_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
