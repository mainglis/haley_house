<h1>Listing event_attendance_records</h1>

<table>
  <tr>
    <th>Individual</th>
    <th>Organization</th>
    <th>Event</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @event_attendance_records.each do |event_attendance_record| %>
  <tr>
    <td><%= event_attendance_record.individual_id %></td>
    <td><%= event_attendance_record.organization_id %></td>
    <td><%= event_attendance_record.event_id %></td>
    <td><%= link_to 'Show', event_attendance_record %></td>
    <td><%= link_to 'Edit', edit_event_attendance_record_path(event_attendance_record) %></td>
    <td><%= link_to 'Destroy', event_attendance_record, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event attendance record', new_event_attendance_record_path %>
