<%= form_for(@event_attendance_record) do |f| %>
  <% if @event_attendance_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_attendance_record.errors.count, "error") %> prohibited this event_attendance_record from being saved:</h2>

      <ul>
      <% @event_attendance_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :individual_id %><br />
    <%= f.number_field :individual_id %>
  </div>
  <div class="field">
    <%= f.label :organization_id %><br />
    <%= f.number_field :organization_id %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.number_field :event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
