<%= form_for(@individual_role_record) do |f| %>
  <% if @individual_role_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@individual_role_record.errors.count, "error") %> prohibited this individual_role_record from being saved:</h2>

      <ul>
      <% @individual_role_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :individual_id %><br />
    <%= f.number_field :individual_id %>
  </div>
  <div class="field">
    <%= f.label :role_id %><br />
    <%= f.select :role_id, @roles %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br />
    <%= f.date_select :end_date, :include_blank => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
