<% target = [@individual, @note] %>
<%= form_for(target) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <!-- <div class="field">
    <%= f.label :individual_id %>
    <%= @individual %>
  </div> -->
  <%= f.hidden_field :individual_id, :value => params[:individual_id].to_i %>

  <div class="field">
  <!--   <%= f.label :note %><br /> -->
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit 'Save', :class => 'btn' %>
  </div>
<% end %>
