<%= form_for(@individual_skill_record) do |f| %>
  <% if @individual_skill_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@individual_skill_record.errors.count, "error") %> prohibited this individual_skill_record from being saved:</h2>

      <ul>
      <% @individual_skill_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :individual_id %><br />
    <%= f.number_field :individual_id %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br />
    <%= f.number_field :skill_id %>
  </div>
  <div class="field">
    <%= f.label :date_gained %><br />
    <%= f.date_select :date_gained %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
