<%= form_for(@individual_organization_record) do |f| %>
  <% if @individual_organization_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@individual_organization_record.errors.count, "error") %> prohibited this individual_organization_record from being saved:</h2>

      <ul>
      <% @individual_organization_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :individual_id %><br />
    <%= f.number_field :individual_id %>
  </div>
  <div class="field">
    <%= f.label :organization_id %><br />
    <%= f.number_field :organization_id %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.date_select :start_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
