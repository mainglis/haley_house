<%= form_for(@roles_roster) do |f| %>
  <% if @roles_roster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roles_roster.errors.count, "error") %> prohibited this roles_roster from being saved:</h2>

      <ul>
      <% @roles_roster.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :individual_id %><br />
    <%= f.number_field :individual_id %>
  </div>
  <div class="field">
    <%= f.label :role_id %><br />
    <%= f.number_field :role_id %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br />
    <%= f.date_select :end_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
